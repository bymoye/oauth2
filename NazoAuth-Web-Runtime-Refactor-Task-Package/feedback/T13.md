# T13 — 执行反馈

状态：PASS — 统一验收完成。官方 OIDF 按用户修订由用户手动执行，记录为 USER-OWNED / NOT RUN；发布和部署另行记录。

## 实施范围与归属

- Research baseline：`ff344fc37d36231ac7e648fd24d0bc657643327f`；T02–T13 起点：`206252c03165995c3bd845bedaa1d121c42330bc`。生产代码提交：`33acf5ba54082ac199c250bbe80be5d30ea73c04`。
- 两个既有静态脚本共用角色分类事实；检查 21 个 workspace package 的依赖方向、alias、path、optional、target/build/dev 依赖与源码执行能力泄漏。
- Canonical registry 覆盖 115 个符号，包含后续归位的 SCIM/UserInfo 契约。原 source policies 均保留并映射到 App/Native 真实路径。
- App 新增独立 integration targets：application_boundaries、host_replacement_contract、security_facts。Host Replacement 仅通过 public App API 和语义 Ports，覆盖 HTTP 型边界、实际 DPoP/replay security facts 和 CIBA one-shot batch；不引用 Native、Actix、Tokio。
- 迁移测试显式装载；共享纯测试 fixture 不重编另一层生产源。删除无消费者旧 fixture 和仅为测试公开的私有 helper。
- 原 missing/inactive HTTP shape 与无效外部签名/子孙进程清理的联合断言保留。prompt-none 的内部原始 claims 保存与公开请求合法字段投影分别验证，未放宽协议过滤。

## 已有验证证据

证据根：`D:/self/.worktrees/`。运行日志只代表对应源码状态；最终结果见 [43 项验收记录](FINAL-ACCEPTANCE.md) 和 [性能报告](PERFORMANCE.md)。

| 检查 | 已确认结果与证据 |
|---|---|
| Python 全部 source/negative guards | 137 PASS，5.908s，`nazoauth-t13-python-final3.log` |
| `verify_static_contracts.py --check` | exit 0，`nazoauth-t13-static-final.log` |
| 实际 persistence dependency graph | exit 0，`nazoauth-t13-dependencies.log` |
| registry/git lock 元组 | 相对研究基线 added=[]、removed=[]；21 members，`nazoauth-t13-lock-identity.json` |
| App Host Replacement / smoke / security facts | 最终全量分别 3/2/3 PASS。额外按 L3.1 单独 `cargo check --locked -p nazo-oauth-server --lib` 与 `cargo test --locked -p nazo-oauth-server --lib --test application_boundaries --test host_replacement_contract`：295+2+3 PASS，exit 0，`nazoauth-t13-host-independent.log` |
| 最新定向修复 | DCR 7 PASS；内部 prompt-none payload 1 PASS，`nazoauth-t13-repairs.log`。随后误指定 PG package 的命令 exit 101，不作为产品失败或通过证据 |
| PG 认证/身份仓储 | 18+36 PASS，`nazoauth-t13-repairs2.log` |
| Windows Native 全部 unit | 1239 PASS、0 FAIL、7 原有 ignored，272.63s，`nazoauth-t13-repairs2.log` |
| 租户目录多实例收敛 | 1 PASS，35.05s，`nazoauth-t13-repairs2.log` |
| 最终 fmt/check/clippy | PASS，check 27.37s、clippy 12.80s，`nazoauth-t13-final-gate.log` |
| Windows 最终 workspace | 2879 PASS、1 次数据库连接失败、7 原有 ignored；失败目标使用同次全量生成的 `identity_repositories-9ba9efab1f671c0e.exe` 复验 36/36 PASS，exit 0，`nazoauth-t13-identity-final-exact-binary.log`。原全量命令 exit 101 保留，不改标为成功 |
| 实际 test list | 两平台 exit 0；Windows 2887、Linux 2896，`nazoauth-t13-final-test-list.log`、`nazoauth-t13-linux-test-list.log`；`nazoauth-t13-test-inventory-evidence.md` 已解释所有基线差异 |
| Linux 全 workspace | 2889 PASS、0 FAIL、7 原有 ignored，exit 0，`nazoauth-t13-linux-docker-full2.log`；原源码归档 SHA256 `b72243f496dc3074bad39edaeb3ae5d7fedb8a617a62f634c13154955cc33875`，manifest `nazoauth-t13-source-final-manifest.json`。运行后只整理任务反馈，无生产源码变化 |

此前全量失败均已逐项修复并保留失败日志：夹具把空 pre-authorized code 错表示为 `Some("")`；DCR fake 未保留存储 client ID；PG/Native source-policy 使用迁移前路径；prompt-none fixture 请求不可由公开协议投影保留的 `sid`；多进程测试环境缺少 CI signing wrapping key。最后一项属于环境配置。不得把失败日志改标为 PASS。

## 不变量核对

- PG、Valkey、schema/migration/checksum 底层实现相对研究基线未变；当前 JWKS cache 与研究基线旧路径文本一致。remote documents 只有 owner/import/Port 转发变化，未修改缓存算法。
- 依赖版本、source/checksum 不变；未增加 crate、兼容重导出、全局生产 lint 抑制、新 ignore/skip 或提高运行时 timeout。
- 源码 inventory 与两平台实际 `--list` 已交叉核对，无不明未装载模块。
- Linux 原 CNB 工作区在验证中关闭，最终结果不可恢复，旧运行不标通过。改用本机 Docker Linux 原生进程环境，Rust 1.98.0 与 CI 固定摘要一致；独立 PostgreSQL 18.6/Valkey 8.1.9 使用 CI 固定镜像；S3 使用已核对摘要的 MinIO 和随机对象名。

## 最终补验

- T00–T13 统一验收 PASS；K01–K43 逐项命令、结果及用户修订见 FINAL-ACCEPTANCE.md。
- 原 SCIM 11 cases、5×400 requests/32 concurrency 和两项 Valkey outage：基线及当前全部 workload/cleanup exit 0。当前在清理全部 workspace release 产物后重建并补验通过，最终证据为 `nazoauth-t13-http-evidence/evidence-current-clean`。
- 五组同环境、同快照配对性能 PASS：吞吐 -0.544%、P99 -1.713%、CPU -0.292%、RSS +0.132%；每轮签名 backend dispatch 200 次。具体测量边界、原 harness 修正和逐轮数据见 PERFORMANCE.md/json。
- 官方 OIDF：用户于 2026-09-12 明确要求跳过，由用户手动执行。USER-OWNED / NOT RUN，不是认证通过。
- 用户追加授权部署至 Hostinger。正式发布与生产部署尚未执行，不把本地重构验收等同于上线完成。

Linux Docker 首次完整运行暴露两个环境前置：access_requests 需要预先完成数据库迁移；租户目录测试的代理只解析数字 IP。已核实 78 个迁移与 users 表，改用同一 Valkey 实例的实际 IP 后，完整重跑 exit 0。未改生产代码或放宽测试条件。Windows 同次构建目标复验前的 Cargo target-only 尝试因触发不必要的 feature 组合重编译而主动终止，未将终止算作测试结果。

最终判定：PASS（适用用户将官方 OIDF 移交手动执行的明确修订）。
