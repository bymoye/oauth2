# T01 — 执行反馈

**最终判定：`PASS`**

## 1. 执行身份

- Baseline: `ff344fc37d36231ac7e648fd24d0bc657643327f`
- Start commit: `c1a03f5d` (T00 PASS).
- End code commit: `71f077fd`; independent baseline fixture fix: `b0112076`.
- Executor: Codex; 2026-09-12.

## 2. 实际改动

- Mechanically moved the old server source tree into `crates/nazoauth/src` and its 136 test files (including T00 golden) into Native tests, preserving relative structure.
- App now contains only `lib.rs` and `ports/{mod,persistence,transient_state,fapi_replay}.rs`.
- Native now owns its lib/main and `launchers/{mod,postgres,valkey,object_store}.rs`; launcher traits remain uniquely owned by Native `cli`.
- Moved PostgreSQL launcher/operator implementation, Valkey connect/config, object-store provider/config selection out of Adapter packages. Actual persistence/state/S3 implementations remain there.
- Moved seven object-store provider unit cases and provider integration coverage to Native. No test fixture copied.
- Updated static/path guards and Python tests; Containerfiles already copy the complete crate tree and final binary, so no path change is necessary.

## 3. Symbol / Ownership

- Canonical provider/binding/CIBA/state/cache types live in App Ports; accessors made public directly.
- FAPI replay future uses standard boxed Send Future; old Native FAPI definitions/re-exports removed.
- `OperatorPersistence`, CLI/config and launchers remain Native; no Adapter→Native or App→Host/Adapter dependency.
- Audited all 182 old source paths: no missing move, no obsolete migrated path. No legacy Host import match remains.

## 4. Cargo / Dependency

- Native inherited original concrete dependencies and test features; App retains only dependencies required by its neutral Ports.
- Adapter launcher-only dependencies removed. Workspace membership unchanged.
- Lock comparison confirms all registry/git versions, sources and checksums unchanged; only workspace dependency lists moved.
- Existing reqwest 0.12 dev dependency is named `reqwest_012` for original Native integration consumers; normal reqwest remains 0.13. No version was upgraded/unified. Rust rejected the same external crate name with both rmeta candidates, making this dependency alias necessary.

## 5. Verification

| Command | Result | Evidence |
|---|---|---|
| Directed unlocked check of Native + three Adapters | PASS | 26.08s; `nazoauth-t01-check.log` |
| Registry/git lock identity comparison | PASS | all name/version/source/checksum tuples identical |
| `cargo check --locked -p nazo-oauth-server -p nazoauth --all-targets` | PASS | 22.06s; `nazoauth-t01-targets.log` |
| `python scripts/verify_static_contracts.py --check` | PASS | exit 0 |
| `python scripts/check_persistence_dependency_graph.py` | PASS | database/transient/object isolation confirmed |
| `python -m unittest discover -s tests/unit -v` | PASS | 101 tests, 5.074s |
| `cargo test --locked -p nazoauth --lib -- --list` | PASS | 1484 tests loaded, no reduction; `nazoauth-t01-list.log` |
| `cargo test --locked -p nazoauth --lib` | PASS | 1477 passed, 0 failed, 7 pre-existing ignored; 234.50s; `D:\self\.worktrees\nazoauth-t01-full-repaired.log` |
| `cargo test --locked -p nazo-postgres --test migrations` | PASS | 16 passed, 0 failed; 2.03s; `D:\self\.worktrees\nazoauth-t01-migrations.log` |
| Legacy Host import scan | PASS | no production match; rg exit 1 as expected |
| `git diff --cached --check` | PASS | exit 0 |

## 6. Invariants

No route, command, binary name, environment/config schema, tenancy, storage schema or security policy intentionally changed. Original provider/launcher method bodies were compared to the prior commit and preserve behavior apart from owner/import/config.validate changes. No compatibility wrapper, duplicate canonical contract, reverse dependency or unsafe Send added.

## 7. Deviations

Cargo dev dependency alias described above retains both existing reqwest versions. Independent commit `b0112076` fixes two synchronous test-server joins in the baseline remote-document fixture by moving the joins to spawn_blocking. Earlier full runs were interrupted at that test; the repaired complete suite passed without production changes, skips or raised timeouts. No architectural deviation.

## 8. Pending

All mandatory T01 checks passed on Windows with PostgreSQL 18.4 and Valkey. Additional isolated Linux Native validation passed: 1481 passed, 0 failed, 7 pre-existing ignored, 156.29s (four Unix-only cases beyond Windows). Evidence: /tmp/nazoauth-t01-validation/native-lib.log; tested isolated T01 snapshot, not later T02/T03 edits. Official OIDF and comparable performance evidence remain pending as recorded in T00.

## 9. Stable successor facts

T01 is PASS and eligible as a prerequisite. App owns neutral Ports, Native owns the migrated execution tree and launcher traits/implementations; Native loads all 1484 unit cases, with no reduction. No legacy Host import remains.

## 10. Final verdict

```text
PASS
```
