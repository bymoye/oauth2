# T04 — 执行反馈

## 统一验收结论（2026-09-12）

**PASS — 本任务的实现与局部验收完成。** 最终真实 HTTP、性能和部署仍在 T13 跟踪，不以本任务 PASS 代替整项交付完成。

- Linux 全 workspace：2889 passed、0 failed、7 原有 ignored，命令 `cargo test --workspace --all-features --locked --no-fail-fast` exit 0；证据 `D:/self/.worktrees/nazoauth-t13-linux-docker-full2.log`。
- Windows Native：1239 passed、0 failed、7 原有 ignored。最终全 workspace 仅 identity_repositories 一次连接数据库失败；用该次全量构建生成的原测试程序复验，36/36 PASS，exit 0。保留原失败日志，不将其退出码改标成功；证据 `nazoauth-t13-final-gate.log`、`nazoauth-t13-identity-final-exact-binary.log`。
- fmt、全 workspace/all-targets/all-features check 与 clippy `-D warnings` exit 0；137 项 Python guards、实际 dependency graph、canonical static checks 全部通过。
- 两平台实际 `--list` 均 exit 0；Windows 2887、Linux 2896 项。迁移用例差异均对应平台条件、已有禁用测试或已确认的语义重命名；没有不明未装载模块。详见 `nazoauth-t13-test-inventory-evidence.md`。
- Host Replacement 3、application boundaries 2、security facts 3 项均实际通过；协议/安全/生命周期用例包含在上述完整目标中。外部 OIDF 按用户要求由用户手动执行，未声明认证通过。
- 最终实现提交由 T13 反馈统一记录；此处所述实际运行的生产源码已冻结。

## 以下为实施期间记录

下文保留当时的路径、步骤与局部检查记录；其中“待统一验收”的历史状态由上节结果覆盖。

**状态：`[-] IMPLEMENTED / PENDING UNIFIED ACCEPTANCE`**

T04 实现基于 206252c0 之后连续实施的 T02/T03 工作区；尚未单独提交。按 2026-09-12 用户修订，全量测试统一至最终验收。

## 实际改动与稳定接口

- App services.rs 成为四个 Local service、四个 profile service、四个 protocol service aliases 与 passkey TTL 的唯一 owner；identity 七个已有 Port 增加直接 Arc 转发和七项 dispatch 测试。
- 纯 crypto、client assertion、policy、client JWE/policy/OAuth/claims/rows/tenancy/client attestation 迁入 App。Native 保留 Argon2 密码执行和具体远程请求/cache。
- App SecurityAudit 显式注入；Native TenantSecurityAudit 绑定 tenant，共享原持久化 repository 和唯一队列。startup 的 core.security_audit 被身份、Authorization、Token、UserInfo 使用。
- 原 resolver 实例实现 RemoteRequestObjectResolverPort，client policy 使用 RemoteJwksResolverPort；具体 resolver/cache 移 Native adapters。
- RuntimeModuleRegistry::snapshot_store 仅 clone 原 Arc；App limiter 接收可信 source_ip 字符串并返回 typed OAuthEndpointError；DPoP 接收中立请求事实和显式 audit。
- HTTP presenter 保持原 OAuth/DPoP/pre-authorized headers/body；对应测试和黄金测试已接线，尚待执行。

## Cargo 与验证

- 只添加已在 lock 中的实际中立依赖；无版本升级、新 crate 或 Native 反向依赖。
- `cargo check --locked -p nazo-identity -p nazo-oauth-server -p nazoauth -p nazo-http-actix --all-targets`：PASS，29.31s；日志 `D:/self/.worktrees/nazoauth-t04-check4.log`。
- App 源码扫描 `bootstrap::|crate::settings|crate::adapters|actix_|tokio::|RemoteClientDocumentResolver|ServerRuntimeModuleRegistry|SmtpVerificationEmailDelivery|LoginPasswordVerifier`：零命中。
- 检查后清除了遗留 Native ServerTokenService 重复 alias、无生产消费者的 limiter 转发函数及 unused import；随 T05 集成编译复核。
- 测试、静态守卫、全量行为回归：PENDING UNIFIED ACCEPTANCE，不能据此标 PASS。

## 行为边界

原密码并发/queue/Argon2、审计队列4096/退避/required顺序、resolver限制、limiter count>max 和 DPoP nonce/错误正文均保留原实现；现阶段证明为代码迁移与编译，运行行为证明待统一验收。

## 最终判定

IMPLEMENTED / PENDING UNIFIED ACCEPTANCE
