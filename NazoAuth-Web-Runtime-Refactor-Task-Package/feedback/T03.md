# T03 — 执行反馈

## 统一验收结论（2026-09-12）

**PASS — 本任务的实现与局部验收完成。** 最终真实 HTTP、性能和部署仍在 T13 跟踪，不以本任务 PASS 代替整项交付完成。

- Linux 全 workspace：2889 passed、0 failed、7 原有 ignored，命令 `cargo test --workspace --all-features --locked --no-fail-fast` exit 0；证据 `D:/self/.worktrees/nazoauth-t13-linux-docker-full2.log`。
- Windows Native：1239 passed、0 failed、7 原有 ignored。最终全 workspace 仅 identity_repositories 一次连接数据库失败；用该次全量构建生成的原测试程序复验，36/36 PASS，exit 0。保留原失败日志，不将其退出码改标成功；证据 `nazoauth-t13-final-gate.log`、`nazoauth-t13-identity-final-exact-binary.log`。
- fmt、全 workspace/all-targets/all-features check 与 clippy `-D warnings` exit 0；137 项 Python guards、实际 dependency graph、canonical static checks 全部通过。
- 两平台实际 `--list` 均 exit 0；Windows 2887、Linux 2896 项。迁移用例差异均对应平台条件、已有禁用测试或已确认的语义重命名；没有不明未装载模块。详见 `nazoauth-t13-test-inventory-evidence.md`。
- Host Replacement 3、application boundaries 2、security facts 3 项均实际通过；协议/安全/生命周期用例包含在上述完整目标中。外部 OIDF 按用户要求由用户手动执行，未声明认证通过。
- 最终实现提交由 T13 反馈统一记录；此处所述实际运行的生产源码已冻结。

## 以下为实施期间记录

下文保留当时的路径、步骤与局部检查记录；其中“待统一验收”的历史状态由上节结果覆盖。

## 状态

IMPLEMENTED — 待统一验收（按用户 2026-09-12 连续实施、最终统一回归要求）。Start commit `206252c0` + 已编译闭合的 T02 本地变更。尚未提交本阶段最终验收结果。

## 实际修改与 owner

- key-management 新增 ExternalSignRequest/ExternalKeySigner；ExternalSigningKey 保存 key_ref + Arc 能力，ActiveSigningKey::External 替代 ExternalCommand。
- KeyManager::load_or_create_database(settings, external_signer, tenant_id, repository, wrapping_keys)；第二参数 Option<Arc<dyn ExternalKeySigner>>。
- DatabaseKeysetBinding 持能力，所有 load/update/list/validate/refresh/VC 状态路径复用；无能力加载 active external-command 时 fail closed。数据库字符串保持 external-command，无 migration。
- 外部返回字节仍经 key 层现有公钥/JWK/算法/原始输入验证。
- KeySettings 只保留 rotation_interval/prepublish_window/verification_grace；key 层删除 watch/run_lifecycle/stop_lifecycle 与 normal Tokio/process-wrap。
- Native adapters/external_signer.rs 拥有 CommandExternalKeySigner；原 32 shared slots、JSON v1、整体 deadline、输出预算、进程树取消及 5 秒 reaper 保持。
- Native jobs/key_lifecycle.rs 持 Sender/JoinHandle，首次等正常间隔，成功重置/失败 1–60 秒退避，VC 上限 30 秒。stop(self).await 不取消在途 refresh。
- Settings/config_loader、keyctl、tenant_resource_preparation、tenant_runtime 接 signer/Task。租户退出并行发出 cooperative key stop 与其它 worker 停机并 await，保持所有权至结束。
- Native/PG tests 更新接口；key 测试使用 fake signer，进程和调度测试迁 Native，新增真实 KeyManager paused-clock/in-flight stop 断言。

## 当前证据

- `cargo check -p nazo-key-management -p nazoauth --all-targets`：PASS，48.69s。定向同步 lock；日志 D:\self\.worktrees\nazoauth-t03-check.log。两条测试 unused import 已删除。
- 全部 registry/git name/version/source/checksum 与 HEAD 一致；只变 workspace 依赖清单。
- key-management/src 扫描 tokio/process_wrap/run_lifecycle/stop_lifecycle 无命中。
- Native dev 开启既有 Tokio test-util；key dev 保留原 async executor 并用 futures-executor 执行新增纯语义 fixture。

## 待统一验收

Key lib/mtls_trust、Native external_signer 和 key_lifecycle（Windows/Linux）、Native 全量及静态门槛均待执行。不得用编译结果代替平台进程取消证明。

## 后继事实

KeyManager refresh/public material/snapshot/health 接口保留；Native 使用既有 openid4vc_public_material() 判断 cadence，无新增 runtime callback。T04 可消费上述语义能力。

最终判定：PASS（统一验收证据见本文首节及 T13 反馈）。
