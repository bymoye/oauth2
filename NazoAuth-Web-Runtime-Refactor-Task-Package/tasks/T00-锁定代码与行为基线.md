# T00 — 锁定代码与行为基线

**状态：`[ ]`**

## Required Context

执行本任务前只加载下列任务包文件；不要预读未来任务。

- `../00-GOAL.md`
- `../01-ARCHITECTURE.md`
- `../02-EXECUTION-PROTOCOL.md`
- `../references/tasks/T00-FILE-MAP.md`
- `../references/tasks/T00-INTERFACES.md`
- `../references/STATIC-ARCHITECTURE-GUARDS.md`
- `../references/TEST-REGRESSION-STRATEGY.md`

## 状态与反馈契约

开始时将 `TASK-INDEX.md` 中 `T00` 从 `[ ]` 改为 `[-]`。
只有本任务全部验收完成并填写 `feedback/T00.md` 后，才能改为 `[x]`。
若阻塞，改为 `[!]` 并在反馈中记录真实阻塞；不得绕过前置继续后续任务。

---
## 1. 任务目标

记录可比较的代码、HTTP和安全行为，防止后续将行为变化误记为纯重构。

## 2. 前置条件

无前置阶段；基线SHA与上传archive固定。实施时HEAD不同必须先核对关键文件diff，不能默默替换基线。

## 3. 精确修改范围

新增docs/project/web-runtime-refactor-baseline.md；新增旧server/tests/unit/http/framework_boundary_transport.rs及对应cfg(test)模块接入。生产行为不改。记录根Cargo.lock、各Cargo.toml、现有CI配置的hash。

## 4. Symbol 级修改方案

只增加baseline/golden fixture与测试装载，不改变任何service签名。保存J3列出的真实HTTP分支及Port调用顺序；区分现有测试与新测试。

## 5. 数据流变化

现有HTTP → 原Application/服务 → 原Port；本阶段只有旁路测试观察，不插入production wrapper。

## 6. 必须保持的行为不变量

所有status/header/body/redirect/cookie、store调用顺序、回放字节、安全错误优先级保持原样。

## 7. 禁止实现方式

不得先修改业务迎合golden；不得升级依赖绕过环境问题；不得把未执行的外部suite标绿。

## 8. 严格实施步骤

Step 1：保存SHA/manifest/lock/source哈希并确认工作树。Step 2：运行已有静态/Python/Rust组。Step 3：添加J3关键golden并运行。Step 4：记录官方suite与负载基线、环境阻塞，再提交测试与说明。

## 9. 每一步局部验证

```bash
# Step 1
git rev-parse HEAD
git status --short
# Step 2
python scripts/verify_static_contracts.py --check
python -m unittest discover -s tests/unit -v
cargo check --locked --workspace --all-targets --all-features
cargo test --locked -p nazo-http-actix
cargo test --locked -p nazo-oauth-server --lib
# Step 3：必须能看到新增test名称，不能只看exit code
cargo test --locked -p nazo-oauth-server --lib -- --list
cargo test --locked -p nazo-oauth-server --lib framework_boundary_transport
# Step 4
python scripts/transport_mode_parity.py --self-test
```

## 10. 任务最终验收标准

基线有SHA、环境及明确成功/失败/未运行结果；golden实际装载；生产改动只限cfg(test)声明。

## 11. 失败处理

先查工具链/libpq/pinned git下载与原基线是否本来失败。基线真实失败先停P0并记录；不将修原bug混进重构。回退当前fixture Step，不改变源行为。

---

## 12. 完成反馈

完成本任务后必须填写 `../feedback/T00.md`。反馈是后续任务唯一需要读取的历史执行摘要。
